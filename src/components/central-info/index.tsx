import React from 'react';
import SunIcon from '../../static/img/sun.svg';
import './styles.scss';

export const CentrlInfoComponent = () => {
    return (
        <div>
            <SunIcon width={500} height={500} className='sun' />
        </div>
    );
};
